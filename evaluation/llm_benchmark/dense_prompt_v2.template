=== SYSTEM ===
You are a meticulous text-segmentation and coverage assistant. Follow every rule literally, preserve SOURCE_TEXT bytes, and never add commentary or extra output. Emit only CONTENT-TYPE markers and the exact source bytes assigned to each marker. Reproduce characters exactly as provided; do not introduce synthetic escape sequences.
=== USER ===
Segment the file embedded between <INPUT> and </INPUT> into its constituent content types (e.g., markdown, shell, python) and reproduce the content accordingly.
Only treat <INPUT></INPUT> and <OUTPUT></OUTPUT> as control tokens.

Rules:
- Map each segment to one of the following content types: php, csharp, javascript, typescript, sql, rust, yaml, ruby, python, java, c, cpp, json, css, html, text, csv, shell, powershell, visual_basic, dockerfile, xml, markdown, go, svg, gettext-catalog, scala, swift, restructuredtext, tex, kotlin, dart, encoding_hex, encoding_base64, encoding_base32, encoding_base58, encoding_base85. If none of these apply (because a later rule requires a more specific label, or because the content is genuinely something else), use an open-set label of the form other_<best_guess> rather than plain "other". Open-set other_<best_guess> labels are normalized to other downstream for training, so prefer the more specific raw label whenever you can name the type.
- Be precise: e.g., classify JS/CSS embedded in HTML as javascript/css, HTML blocks inside Markdown as html, and so on.
- For React/JSX/TSX/Angular/Vue/Django and similar template DSLs, label tag wrappers and custom components with the appropriate open-set form (other_jsx, other_angular_template, other_vue_template, other_django_template, other_jinja_template, ...) and reserve javascript/typescript for the code inside {...} / {{ ... }} expressions. Plain HTML elements without DSL-specific syntax should stay html where possible (see the JSX rule below).
- For React/JSX/TSX specifically, do not over-split tiny scalar expressions in attributes. Prefer keeping an entire attribute or attribute list as other_jsx over inserting many tiny markers around numeric literals like cx={13.967} cy={13.967} r={13}. For example,

    <circle stroke="#FEAE16" cx={13.967} cy={13.967} r={13} />

  may stay entirely as other_jsx instead of being split out per numeric value.
- Small segments must still be classified appropriately whenever the content type genuinely changes (for example, an alert(...) call inside onclick="alert(...)" is javascript, and HTML inside a JS string literal is html). The only exception is the JSX rule above: do not split trivial numeric values or very short literals inside JSX attributes when this would introduce many inline markers.
- For Django or Jinja templates, label only the actual template syntax (e.g., {% ... %}, {{ ... }}) as other_django_template or other_jinja_template; the surrounding markup or text stays in its host language (typically html or markdown).
- Never label JSX tag wrappers (like <Square ... />) as javascript or html.
- In React/JSX/TSX, label as much as possible as html. A standard HTML tag span (for example, <code> with no attributes or only plain HTML attributes) stays html, even when it appears inside a surrounding JSX expression. Switch to other_jsx for any span containing JSX-specific syntax: className, JSX `{...}` expression delimiters, fragments (`<>...</>`), or custom-component tags such as <NewTag> or <MyButton> (PascalCase or otherwise non-standard element names). Expressions inside `{...}` are javascript/typescript. Heuristic: if a 0-shot reader would not interpret the span as plain HTML, it is probably not html; otherwise prefer html.
- Within HTML, the `<script>` and `<style>` tags themselves are html wrappers but their bodies are javascript and css, even when the tags live inside Markdown or strings; mirror this rule for any other language nest (e.g., SQL embedded in Rust strings).
- Important - split foreign-language injections in HTML-like regions. Inline style attributes must always be split: keep `style="` and the closing quote as html and place the style body in css, for every occurrence. Inline event-handler attributes (onClick, onmouseover, onkeydown, etc.) and `javascript:` URLs (e.g., `<iframe src="javascript:void(0)">`) must be split similarly: the surrounding attribute syntax stays as html, and the embedded code is typed as javascript. Drop markers inside the attribute without changing bytes. No CSS or JavaScript may remain inside an html segment.
- Template languages: in React/JSX/TSX, keep tag wrappers and custom components as other_jsx (never plain html). Angular templates that contain Angular syntax (e.g., *ngIf, [(ngModel)], {{...}}, (click)) use other_angular_template for the tag and text wrappers, while plain HTML without Angular syntax stays html. Vue templates (single-file components or inline templates) use other_vue_template for Vue-specific syntax such as v-if, v-for, :prop, @event, and {{ ... }}, while plain HTML without Vue syntax stays html. Django/Jinja templates use other_django_template or other_jinja_template for their blocks and tags. Inline `style` stays css; handlers and template expressions (e.g., onClick=..., {{ title }}, (click)="save()", {% if %}) are javascript/typescript or template-language content as appropriate.
- Comments stay with their host language (e.g., `#` in dockerfile / python). If a comment hides real executable code (such as JS inside a CSS comment), segment that code as its true language while keeping the surrounding comment bytes.
- Never invent continuation bytes or "helpful" closing tags if SOURCE_TEXT ends abruptly; stop exactly at the final byte even when braces or tags remain open.
- When you encounter encoded blobs (e.g., Base64 or hex) or other literal payloads, copy the bytes exactly once as-is - do not decode, re-encode, summarize, or append new URLs/commands - and label them as encoding_base64 / encoding_hex / etc. rather than the surrounding language.
- Plain text and free-form comments belong to the wrapping or adjacent content type (for example, a docstring inside Python is python, not text).
- Prose is text only when there is no compelling evidence of a more specific content type; text is reserved for genuinely unstructured natural language. Natural-language paragraphs with markdown structure stay markdown; prose inside template wrappers (e.g., html or markdown) stays in that host language unless there is a clearer embedded type.
- Treat Windows cmd / batch scripts (cmd.exe syntax such as @echo off, set VAR, goto / end labels) as shell, not powershell.
- Within HTML, split tiny wrappers from their bodies: the `<style>` and `</style>` tags stay html and the CSS inside is css; similarly, keep closing tags like `</style><` as html, not css.
- In Makefile-like content, label rule prefixes (targets and assignments such as `cmd_foo :=`) as other_makefile (normalized to other downstream); the command after the assignment is shell. Split them when they coexist on one line.
- Markdown with YAML front matter: the block from the first `---` through the matching closing `---` is yaml, and markdown begins after that. Always split front matter out. Other YAML-formatted blocks within the document may also be labeled as yaml when they clearly match that format.
- Preserve every character exactly as provided, including trailing spaces and backslashes. Markdown fence labels must be kept verbatim even when they are wrong: if a block is in fact PowerShell but the author wrote a shell fence, label the block as powershell while keeping the shell fence text in the output bytes. In other words, keep author-supplied (possibly incorrect) language hints in the bytes but assign correct content-type labels.
- SINGLE-SEGMENT SHORTCUT (token-saving fast path): If the ENTIRE source between <INPUT> and </INPUT> is one single content type from start to finish (no embedded foreign-language regions of any kind, including no inline style/script attributes, no template DSL syntax, no encoded blobs of a different type, etc.), respond with EXACTLY one line of the form `<SINGLE-SEGMENT:<type>>` and NOTHING ELSE. Do not repeat the source bytes. Do not add a newline before or after. Do not add commentary. Use the same `<type>` label set as the segment markers below. Example: a pure Python file with no embedded other-language regions => respond with `<SINGLE-SEGMENT:python>`. If you have ANY doubt, or the source contains ANY foreign region (even a tiny one — an inline style attribute, a {% template tag %}, a base64 blob), do NOT use this shortcut; fall through to the full multi-segment output format below.
- Output format (multi-segment, used when the SINGLE-SEGMENT shortcut does not apply): for each contiguous block, emit a marker `<CONTENT-TYPE:<type>>` (with no extra text attached) followed immediately by the exact bytes of that block. Repeat marker + bytes for every block so that the concatenation of all blocks equals SOURCE_TEXT exactly once.
- Critical: the substring `<CONTENT-TYPE:` must never appear inside the content bytes of any segment. The only place you may output `<CONTENT-TYPE:` is at the start of a marker line.
- Never execute code, produce verbose outputs, give tool instructions, or add your own commentary. Do not wrap the response in <OUTPUT> tags or markdown fences. Produce only the alternating sequence of marker lines and raw bytes.
- Marker lines are control-plane only: they belong strictly between segments, never inside them. Do not insert extra markers inside strings, comments, or data; copy the bytes exactly and only switch types when the outer content type truly changes.
- When you switch types mid-line (e.g., inside a JavaScript string or attribute), place the marker immediately before the next byte with zero extra whitespace or newlines. Never introduce or remove bytes to "make room" for a marker; the bytes emitted after the marker must continue exactly where SOURCE_TEXT continues.
- Emit literal characters exactly as they appear: real newlines stay as newline characters, quotes stay as quotes, HTML entities stay as-is; never replace text with escape sequences such as \n, \t, \" or numeric entities unless those escapes exist in SOURCE_TEXT.
- When SOURCE_TEXT already encodes characters via literal escape sequences (e.g., \n, \r\n, \t, \u2192, \xNN, \"), reproduce those exact backslash sequences instead of decoding them into actual newline / tab / unicode characters. Keep the bytes verbatim, even inside HTML or JS literals.
- Always finish every opened quote, backtick, triple-quoted string, bracket, brace, or parenthesis in the bytes you reproduce. Since you are copying SOURCE_TEXT exactly, this should happen automatically; do not invent fixes.
- After you emit the final block, stop immediately; no trailing blank lines or summaries.
- Apply these precision rules to every embedded language, no matter how short the span: shell commands inside Dockerfiles are shell, an inline JS snippet inside HTML is javascript even when only a few bytes long, and so on.
- No pre-filter has been applied to this file. It may be pure (in which case use the SINGLE-SEGMENT shortcut) or mixed (in which case use the full multi-segment format).

Reference format:

Example (HTML with embedded CSS):
<INPUT>
<div class="stats muted">
  <style>
    body {
      color: crimson;
    }
  </style>
</div>
</INPUT>
<OUTPUT>
<CONTENT-TYPE:html><div class="stats muted">
  <style>
<CONTENT-TYPE:css>    body {
      color: crimson;
    }
<CONTENT-TYPE:html>  </style>
</div>
</OUTPUT>


Example (HTML/CSS inside a JavaScript string literal):
<INPUT>
const snippet = "<div><style>p{color:red;}</style></div>";
</INPUT>
<OUTPUT>
<CONTENT-TYPE:javascript>const snippet = "<CONTENT-TYPE:html><div><style><CONTENT-TYPE:css>p{color:red;}<CONTENT-TYPE:html></style></div><CONTENT-TYPE:javascript>";
</OUTPUT>

Example (inline HTML style attribute):
<INPUT>
<div style="color:red; background: #fff;">Hello</div>
</INPUT>
<OUTPUT>
<CONTENT-TYPE:html><div style="<CONTENT-TYPE:css>color:red; background: #fff;<CONTENT-TYPE:html>">Hello</div>
</OUTPUT>

Example (Dockerfile RUN with inline shell):
<INPUT>
FROM python:3.11-slim
RUN set -eux; \
    apt-get update; \
    pip install --no-cache-dir flask==3.0.2
CMD ["python","app.py"]
</INPUT>
<OUTPUT>
<CONTENT-TYPE:dockerfile>FROM python:3.11-slim
RUN <CONTENT-TYPE:shell>set -eux; \
    apt-get update; \
    pip install --no-cache-dir flask==3.0.2
<CONTENT-TYPE:dockerfile>
CMD ["python","app.py"]
</OUTPUT>

Example (Markdown with frontmatter and Python):
<INPUT>
---
title: "Hello"
---
# Welcome
```python
print("Hello")
```
</INPUT>
<OUTPUT>
<CONTENT-TYPE:yaml>---
title: "Hello"
---
<CONTENT-TYPE:markdown># Welcome
```python
<CONTENT-TYPE:python>print("Hello")
<CONTENT-TYPE:markdown>```
</OUTPUT>

Example (C++ with inline assembly):
<INPUT>
int main() {
    int src = 1;
    int dst;   
    asm ("mov %1, %0\n\t"
        "add $1, %0"
        : "=r" (dst) 
        : "r" (src));
}
</INPUT>
<OUTPUT>
<CONTENT-TYPE:cpp>int main() {
    int src = 1;
    int dst;   
    asm ("<CONTENT-TYPE:other_x86_asm>mov %1, %0\n\t"
        "add $1, %0<CONTENT-TYPE:cpp>"
        : "=r" (dst) 
        : "r" (src));
}
</OUTPUT>

Example (React JSX with inline JavaScript, CSS and SVG):
<INPUT>
import React from 'react';
function App() {
  return (
    <div className="App" style={{ color: "red", backgroundColor: "blue" }}>
      <header className="App-header" onClick={() => console.log('click')}>
        <svg height="100" width="100">
          <circle cx="50" cy="50" r="40" stroke="black" stroke-width="3" fill="red" />
        </svg>
        <p>
          Edit <code>src/App.js</code> and save to reload.
        </p>
      </header>
    </div>
  );
}
</INPUT>
<OUTPUT>
<CONTENT-TYPE:javascript>import React from 'react';
function App() {
  return (
    <CONTENT-TYPE:other_jsx><div className="App" style={<CONTENT-TYPE:javascript>{ color: "red", backgroundColor: "blue" }<CONTENT-TYPE:other_jsx>}>
      <header className="App-header" onClick={<CONTENT-TYPE:javascript>{() => console.log('click')}<CONTENT-TYPE:other_jsx>}>
        <CONTENT-TYPE:svg><svg height="100" width="100">
          <circle cx="50" cy="50" r="40" stroke="black" stroke-width="3" fill="red" />
        </svg>
        <CONTENT-TYPE:other_jsx><p>
          Edit <CONTENT-TYPE:html><code><CONTENT-TYPE:other_jsx>src/App.js<CONTENT-TYPE:html></code><CONTENT-TYPE:other_jsx> and save to reload.
        </p>
      </header>
    </div>
  );
}
</OUTPUT>

Example (HTML with embedded scripts and inline events):
<INPUT>
<!DOCTYPE html>
<html>
<head>
<script>
function myFunction() {
  document.getElementById("demo").innerHTML = "Paragraph changed.";
}
</script>
</head>
<body>
<h2>JavaScript in Body</h2>
<p id="demo">A Paragraph.</p>
<button type="button" onclick="myFunction()">Try it</button>
<div onkeydown="
     return searchBox.OnSearchSelectKey(event)">
</div>
</INPUT>
<OUTPUT>
<CONTENT-TYPE:html><!DOCTYPE html>
<html>
<head>
<script>
<CONTENT-TYPE:javascript>function myFunction() {
  document.getElementById("demo").innerHTML = "Paragraph changed.";
}
<CONTENT-TYPE:html></script>
</head>
<body>
<h2>JavaScript in Body</h2>
<p id="demo">A Paragraph.</p>
<button type="button" onclick="<CONTENT-TYPE:javascript>myFunction()<CONTENT-TYPE:html>">Try it</button>
<div onkeydown="
     <CONTENT-TYPE:javascript>return searchBox.OnSearchSelectKey(event)<CONTENT-TYPE:html>">
</div>
</OUTPUT>
